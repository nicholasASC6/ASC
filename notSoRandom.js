function random (){
    let numbers = Math.random();
    numbers *= 10;
    numbers = Math.ceil(numbers);
    console.log('The random number from 1 to 10 is ' + numbers);
};
random ();

function rando (){
    let no = Math.random();
    no *= 100;
    no = Math.ceil(no);
    console.log('The random number from 1 to 100 is ' + no);
};
rando ();

function rand (){
    let nos = Math.random();
    nos *= 2;
    nos = Math.ceil(nos);
    console.log('The random number from 1 to 2 is ' + nos);
};
rand ();